<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_field :duration %>
  </div>
  <div class="field">
    <%= f.label :year_of_release %><br>
    <%= f.date_select :year_of_release %>
  </div>
  <div class="field">
    <%= f.label :album_title %><br>
    <%= f.text_field :album_title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
